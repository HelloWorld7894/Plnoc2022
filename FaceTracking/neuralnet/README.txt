NOTE: neuralnet directory uses ImageAi API, because ImageAi has some conflicts with 
newer versions of some libraries (mostly tensorflow), i highly recomment creating virtual enviroment
(all requirements are in requirements.txt file)